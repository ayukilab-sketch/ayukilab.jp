# ayukilab.jp スターサーバー → GitHub Pages 移行手順

前提条件の整理：

- 現在のホスティング：スターサーバー（レンタルサーバー）。契約は2027年4月末日まで有効（支払い済み）。
- ドメイン：ayukilab.jp（スタードメインで登録・管理）。ドメインの登録先は変更しません。今回変更するのは「ドメインがどのサーバーを指すか（DNS）」だけです。
- 移行先：GitHub Pages。ソースは既存リポジトリ `github.com/ayukilab-sketch/ayukilab.jp` をそのまま使用します。
- サイトの正式URLはこれまで通り `https://www.ayukilab.jp/`（wwwあり）を使い続ける想定です。既存の全ページの canonical タグが www 付きで統一されているためです。

急いでスターサーバーを解約する必要はありません。契約は2027年4月末まで有効なので、GitHub Pages側で正常動作を確認できるまではスターサーバーを並行稼働させたまま進められます。両方が同時に生きている期間があっても問題ありません（DNSの向き先を切り替えるまでは、旧サーバーがそのまま応答し続けます）。

---

## 現状確認（追記）

いただいたGitHubリポジトリの画面と、実際のURLへのアクセス確認から、以下が判明しています。

- **GitHub Pagesは既に有効化済みです。**「Deployments 7、github-pages」の表示は、Pages機能が既に動作していることを示しています。実際に `https://ayukilab-sketch.github.io/ayukilab.jp/` にアクセスすると、現在のサイト内容がそのまま表示されることを確認しました。つまり下記「手順1」は完了済みで、**手順2から始めれば大丈夫です。**
- **カスタムドメインはまだ未設定です。** リポジトリ内に `CNAME` ファイルが存在しないため、GitHub側の「Custom domain」欄はまだ空欄のはずです。
- サイト内の全リンクは相対パス（`index.html` や `../style.css` など）で統一されているため、カスタムドメイン設定の前後でリンク切れが起きる心配はありません。

---

## 事前に行っておいたこと（今回の作業で対応済み）

GitHub Pagesは静的ファイルをそのまま配信する仕組みで、`.htaccess` のようなApache設定ファイルは一切読み込まれません。リポジトリ内の `.htaccess` は無視されます。

調査の結果、教材のうち以下2件が `.htaccess` のBrotli/Gzip圧縮設定（`Content-Encoding` ヘッダーの付与）に依存しており、このままGitHub Pagesに移行すると**正常に読み込めなくなる**ことが分かりました。

- `kyozai/k_0001.html`（ウインドミル）… `.data.br` `.framework.js.br` `.wasm.br` を参照
- `kyozai/k_0010.html`（交流電源とRLC回路）… `.data.gz` `.framework.js.gz` `.wasm.gz` を参照

この2件については、圧縮ファイルを展開して非圧縮版を作成し、Unityのローダー設定（`kfile/0001/index.html` と `kfile/0010/index.html`）が非圧縮ファイルを読み込むように修正済みです。元の圧縮ファイル（`.br` `.gz`）はリポジトリに残していますが、今後は参照されないため、ある程度動作確認が終わったら削除してもらって構いません（合計約19MB）。

他の教材（k_0002〜k_0009、k_0011）は元々非圧縮、または `.unityweb` 拡張子（`.htaccess` に依存しない形式）のため、追加対応は不要です。

リポジトリの合計サイズは現在 約690MB です。GitHubが推奨する上限（1リポジトリあたり目安1GB）の範囲内ですが、`kyozai/kfile/0006/AR.js-master/` 内にARライブラリのテスト用動画・jarファイルなど、サイトの表示に不要なファイルが多く含まれており、これがサイズの大部分（約180MB）を占めています。容量に余裕を持たせたい場合、この中の `test/` フォルダなど不要な部分の削除を検討できます（削除の実行は指示があり次第対応します）。

---

## 手順1：GitHub Pagesを有効化する　※完了済み

上記の通り既にPagesは有効化され、`https://ayukilab-sketch.github.io/ayukilab.jp/` で公開されています。念のため設定内容を確認したい場合のみ、以下を確認してください。

1. `https://github.com/ayukilab-sketch/ayukilab.jp` → **Settings** タブ → 左メニュー「Code and automation」内の **Pages**。
2. 「Build and deployment」の Source が **Deploy from a branch**、Branch が `main` / `/ (root)` になっていることを確認（問題なければ手順2へ）。

## 手順2：カスタムドメインをGitHubに登録する

1. 同じ Pages 設定画面の「Custom domain」欄に `www.ayukilab.jp` と入力して **Save**。
   - これによりリポジトリのルートに `CNAME` というファイルが自動生成されます（中身は `www.ayukilab.jp` の1行のみ）。ローカルで作業を続ける場合は、このコミットを `git pull` して手元にも反映してください。
2. この時点ではまだDNSが設定されていないため、GitHub側に「DNS check unsuccessful」などの警告が出ますが、次のDNS設定を終えれば解消します。

## 手順3：スタードメインでDNSレコードを設定する

1. [Star8アカウント](https://secure.netowl.jp/starapanel/login/stardomain/) にログイン。
2. ドメイン一覧から `ayukilab.jp` の行、右端のクイックメニューから「DNSレコード設定」を開く。
3. 現在 `ayukilab.jp` と `www.ayukilab.jp` にスターサーバーのIPアドレスを指すAレコードが設定されているはずです。これらを以下の内容に**編集**します（既存のAレコードを削除して以下を追加、または内容を書き換え）。

**① 独自ドメイン（ayukilab.jp）宛のAレコード（4件）**

| ホスト名 | 種別 | 内容 |
|---|---|---|
| ayukilab.jp（空欄でも可） | A | 185.199.108.153 |
| ayukilab.jp（空欄でも可） | A | 185.199.109.153 |
| ayukilab.jp（空欄でも可） | A | 185.199.110.153 |
| ayukilab.jp（空欄でも可） | A | 185.199.111.153 |

**② www サブドメイン宛のCNAMEレコード（1件）**

| ホスト名 | 種別 | 内容 |
|---|---|---|
| www.ayukilab.jp | CNAME | ayukilab-sketch.github.io |

※ CNAMEレコードの仕様上、同じホスト名に対して他の種別（A・MX・TXTなど）のレコードを同時に設定できません。`www.ayukilab.jp` に既存のAレコードが残っている場合は、CNAME追加前に必ず削除してください。

※ 独自ドメイン直下（`*.ayukilab.jp`）のワイルドカードAレコードがデフォルトで設定されている場合、セキュリティ上の理由からGitHub側は無効化を推奨しています。使っていなければ削除を検討してください。

※ メール関連のMXレコード・SPF用TXTレコードは今回変更不要です（ホスティングとメールは別物のため、そのまま残して問題ありません）。

## 手順4：反映確認

DNSの反映には最大24時間かかります。

- ターミナルで `dig ayukilab.jp +noall +answer -t A` を実行し、GitHubの4つのIPアドレスが返ってくることを確認。
- `dig www.ayukilab.jp +noall +answer` を実行し、`ayukilab-sketch.github.io` へのCNAMEが返ってくることを確認。
- ブラウザで `https://www.ayukilab.jp/` にアクセスし、GitHub Pages側のサイトが表示されることを確認。
- 特に以下は個別に開いて動作確認してください：
  - `kyozai/k_0001.html`（ウインドミル・今回修正）
  - `kyozai/k_0010.html`（RLC回路・今回修正）
  - `kyozai/k_0011.html`（電位シミュレーター）
  - `contact.html`（Googleフォーム埋め込み）

## 手順5：HTTPSを有効化する

DNSが反映されると、GitHubのPages設定画面で **Enforce HTTPS** のチェックボックスが選択可能になります（反映後、最大24時間程度かかる場合があります）。有効化して常時HTTPS化してください。

## 手順6：スターサーバーの扱い

- GitHub Pages側での表示・全ページの動作を確認できたら、スターサーバー上のファイルはそのまま残しておいて構いません（DNSの向き先が変わった時点でアクセスされなくなります）。
- 契約更新（2027年4月末）のタイミングで自動更新を停止すれば、追加の作業なく契約終了できます。急いで手動解約する必要はありません。
- 逆に、GitHub Pages側で何か問題が見つかった場合は、DNSレコードを元のスターサーバーのIPアドレスに戻すだけで即座に切り戻せます。

---

## 補足：スターサーバーとの違いで意識しておきたい点

- `.htaccess` は使えません（今回の圧縮設定以外にも、今後 `.htaccess` に何か追記しても効果はありません）。
- サーバーサイドの処理（PHPなど）は一切実行できません。今回のサイトは静的HTMLのみなので影響はありません。
- 独自の404ページを設定したい場合は、リポジトリ直下に `404.html` を置くとGitHub Pagesが自動的に使用します。

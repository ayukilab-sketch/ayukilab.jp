# ファイル一時保管庫（管理者専用アップロード機能）セットアップ手順

サイト訪問者には見えず、岸上様（管理者）のみが動画・写真をアップロード／閲覧できるページです。
URL: `https://www.ayukilab.jp/upload/`（サイトのナビゲーションには載せていません）

- ページ本体：`upload/index.html`（作成済み）
- 接続設定ファイル：`upload/google-config.js`（岸上様がご自身の情報を入力）

**方式（2026-08-07 変更）**：当初 Firebase Storage を検討しましたが、2026年2月からFirebase Storageの利用に
Blazeプラン（従量課金プラン、クレジットカード登録必須）への切り替えが必須化されたため撤回しました。
代わりに **岸上様ご自身のGoogleドライブ** をそのままアップロード先として使う方式に変更しています。

- 追加費用は一切かかりません（岸上様の既存Googleアカウントの無料ドライブ容量をそのまま使用）
- クレジットカード登録・請求先アカウントの作成は一切不要です
- アップロードしたファイルは岸上様ご本人のドライブに保存されるため、ログインしていない
  サイト訪問者はもちろん、他のGoogleアカウントでも中身を見ることはできません

---

## 手順1：Google Cloudプロジェクトを作成

1. https://console.cloud.google.com/ にアクセスし、Googleアカウント（kyoshokuayk@gmail.com）でログイン
2. 画面上部のプロジェクト選択メニュー →「新しいプロジェクト」
3. プロジェクト名（例：ayukilab-upload）を入力 → 作成
   （請求先アカウントの設定は求められません。求められた場合は手順を中断してご連絡ください）

## 手順2：Google Drive APIを有効化

1. 左メニュー「APIとサービス」→「ライブラリ」
2. 「Google Drive API」を検索 →「有効にする」をクリック

## 手順3：OAuth同意画面を設定

1. 左メニュー「APIとサービス」→「OAuth同意画面」
2. User Type は「External（外部）」を選択 → 作成
3. アプリ名（例：AyukiLab 一時保管庫）、ユーザーサポートメール、デベロッパーの連絡先情報に
   kyoshokuayk@gmail.com を入力 → 保存して次へ
4. 「対象」画面はそのまま「保存して次へ」
5. 「テストユーザー」の画面で kyoshokuayk@gmail.com を追加 → 保存して次へ
   ※ この「テストユーザー」に登録したアカウントのみがログインできる仕組みです。
     Googleの審査（公開申請）は不要で、このまま無期限に使えます。
6. 作成後、左メニュー「データアクセス」→「スコープを追加または削除」で
   `https://www.googleapis.com/auth/drive.file` と
   `https://www.googleapis.com/auth/userinfo.email` にチェックを入れて更新
   ※ ここでスコープを明示的に追加しないと、実際のアップロード時に
     「Request had insufficient authentication scopes」エラーになります
     （2026-08-07 の動作確認で実際に発生し、この手順で解決済み）。

## 手順4：OAuthクライアントIDを作成

1. 左メニュー「APIとサービス」→「認証情報」→「+ 認証情報を作成」→「OAuthクライアントID」
2. アプリケーションの種類：「ウェブ アプリケーション」
3. 名前（例：ayukilab-upload-web）を入力
4. 「承認済みの JavaScript 生成元」に以下を追加：
   - `https://www.ayukilab.jp`
   - `https://ayukilab.jp`
5. 「作成」をクリックすると、クライアントIDが表示されます（例：`123456789-xxxx.apps.googleusercontent.com`）

## 手順5：接続設定ファイルに入力

`upload/google-config.js` を開き、以下を編集して保存してください。

```js
window.GOOGLE_CLIENT_ID = "手順4で取得したクライアントID";
window.ADMIN_EMAIL = "kyoshokuayk@gmail.com";
window.DRIVE_FOLDER_NAME = "AyukiLab_一時アップロード";
```

## 手順6：完成ファイルをコミット・プッシュ

保存したら、いつも通りGitHub Desktopで Commit to main → Push origin を行ってください。
（Client IDは公開されても問題ない情報です。実際の保護はOAuth同意画面のテストユーザー設定で行われます）

## 手順7：動作確認

1. `https://www.ayukilab.jp/upload/` を開く
2. 「Googleでログイン」→ kyoshokuayk@gmail.com でログイン
3. 初回はGoogleから「このアプリはGoogleで確認されていません」という警告が出ますが、
   ご自身で作成したアプリのため問題ありません。「詳細」→「（アプリ名）に移動」で進めます
4. ファイルをアップロードし、Googleドライブのマイドライブ内に
   「AyukiLab_一時アップロード」フォルダが作成され、ファイルが保存されることを確認してください

---

## 運用メモ

- 「一時保存」用途のため、不要になったファイルはページ上の「削除」ボタン、または
  Googleドライブアプリから直接削除してください
- 保存容量は岸上様のGoogleアカウントの空き容量に依存します（無料で15GB、Gmail等と共用）
- ログインを許可するアカウントは「テストユーザー」に登録した1件のみです。増やす場合は
  Google Cloud Console の OAuth同意画面でテストユーザーを追加してください
- 動画の長さ・サイズの上限は特に設けていません（Googleドライブの空き容量まで利用可能です）

---

# 外部提出用ページ（第三者がログイン不要でアップロード）

上記の `/upload/` は岸上様ご本人専用（Googleログイン必須）ですが、
「リンクを知っている人なら誰でも、ログインなしでアップロードできる」ページも別途用意しました。
提出されたファイルは、こちらも岸上様のGoogleドライブ（「AyukiLab_外部からの提出」フォルダ）に保存されます。

- ページ本体：`upload/QfPhUhFM8hZVEgrX2TggDpg1/index.html`（作成済み。フォルダ名自体が推測困難な合言葉になっています）
- 送信先設定：`upload/QfPhUhFM8hZVEgrX2TggDpg1/config.js`（岸上様がApps ScriptのURLを入力）
- 中継スクリプト：`upload/gas/Code.gs`（Google Apps Scriptに貼り付けて使用）

**共有用URL（アップロードしてほしい相手にはこのURLを送ってください）**：
`https://www.ayukilab.jp/upload/QfPhUhFM8hZVEgrX2TggDpg1/`

### 仕組みと安全性について

訪問者にGoogleアカウントへのアクセス権を渡すことなく、岸上様のドライブに保存する必要があるため、
Google Apps Script（岸上様の権限で動く無料の中継サーバー）を経由する構成にしています。
大きな動画ファイルにも対応できるよう、ファイルは自動的に細かく分割されて送信され、
Googleドライブ側で1つのファイルに再構成されます。

このページのURLさえ知っていれば誰でもアップロードできてしまうため、
悪用されないよう不用意に公開・拡散しないようご注意ください。もし流出した場合は、
`upload/` フォルダ内の該当フォルダ名を変更し、config.jsのURLも再発行することで無効化できます。

## 手順A：Google Apps Scriptプロジェクトを作成

1. https://script.google.com/ にアクセスし、Googleアカウント（kyoshokuayk@gmail.com）でログイン
2. 「新しいプロジェクト」をクリック
3. プロジェクト名を「AyukiLab提出中継」などに変更（左上のプロジェクト名をクリックして編集）
4. エディタ内のデフォルトコード（`function myFunction() {...}`）をすべて削除し、
   `upload/gas/Code.gs` の内容をすべて貼り付けて保存（Ctrl+S / Cmd+S）

## 手順B：ウェブアプリとしてデプロイ

1. 右上の「デプロイ」→「新しいデプロイ」
2. 「種類の選択」の歯車アイコン →「ウェブアプリ」を選択
3. 説明（任意）を入力
4. 「次のユーザーとして実行」：**自分（Me）**
5. 「アクセスできるユーザー」：**全員（Anyone）**
6. 「デプロイ」をクリック
7. 初回は「このアプリは確認されていません」という警告が出ますが、ご自身が作成したスクリプトのため
   問題ありません。「詳細」→「（プロジェクト名）に移動」→「許可」で、
   このスクリプトが岸上様のGoogleドライブにアクセスすることを承認してください
8. 発行された「ウェブアプリ」のURL（`https://script.google.com/macros/s/.../exec` の形式）をコピー

## 手順C：設定ファイルに入力

`upload/QfPhUhFM8hZVEgrX2TggDpg1/config.js` を開き、以下を編集して保存してください。

```js
window.SUBMIT_ENDPOINT = "手順Bで取得したウェブアプリのURL";
```

保存後、いつも通りGitHub Desktopで Commit to main → Push origin してください。

## 手順D：動作確認

1. `https://www.ayukilab.jp/upload/QfPhUhFM8hZVEgrX2TggDpg1/` を（ログインしていないブラウザや
   シークレットウィンドウで）開く
2. ログイン画面が出ないことを確認し、テスト用の動画・画像をアップロード
3. 岸上様のGoogleドライブに「AyukiLab_外部からの提出」フォルダが作成され、
   ファイルが保存されていることを確認してください

### 運用メモ（外部提出用ページ）

- Apps Scriptの無料枠には1日あたりの実行時間・URLフェッチ回数の上限がありますが、
  個人利用の範囲では通常問題になりません
- Code.gs を修正した場合は、script.google.com で「デプロイ」→「デプロイを管理」→
  既存デプロイの編集アイコン →「新バージョン」を選んで再デプロイしないと反映されません
- 大容量ファイル（1GBを超えるような動画）は分割回数が多くなり時間がかかるため、
  提出者にはできるだけ圧縮・トリミングしたものを送ってもらうことをおすすめします

---

## 参考：以前作成したFirebaseプロジェクトについて

セットアップ途中で `ayukilab-storage` という名前のFirebaseプロジェクトを作成しましたが、
Storage機能はBlazeプラン必須のため使用を取りやめました。放置しても料金は一切発生しません
（Authenticationを試験的に有効化した程度で、Storageは未作成のままです）。
不要であれば Firebaseコンソール（https://console.firebase.google.com/）からご自身で削除いただけますが、
残しておいても無害です。
